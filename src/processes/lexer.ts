import { tokens, _2dArray } from "src/typescript/types";

export class lexer {
  arr: _2dArray;
  tokens: tokens = [];
  constructor(arr: _2dArray) {
    this.arr = arr;
  }
  tokenizer():tokens{
    
    return this.tokens
  }

}
